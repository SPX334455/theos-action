name: Build Tweak

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4 # v4 yaptık

      - name: Build Tweak
        uses: Randomblock1/theos-action@v1
        with:
          theos-sdks: 'https://github.com/theos/sdks'

      build
No files were found with the provided path: .theos/obj/debug/DilSihirbazi.dylib. No artifacts will be uploaded.
